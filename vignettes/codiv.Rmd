---
title: "Running a codiversification scan"
author: "Daniel Sprockett"
date: "2025-02-12"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Codiversification Scan

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r load packages}

require(ape)

```


```{r import data}

# import input files
Host_tr <- read.tree("/Users/danielsprockett/Software/codiv/data/Host_Tree.nwk")
Host_tr 

Symbiont_tr <- read.tree("/Users/danielsprockett/Software/codiv/data/Symbiont_Tree.treefile")
Symbiont_tr 

Host_to_Symbiont_df <- read.table("/Users/danielsprockett/Software/codiv/data/Host_to_Symbiont.txt", sep = "\t", header = TRUE)
dim(Host_to_Symbiont_df)


```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
